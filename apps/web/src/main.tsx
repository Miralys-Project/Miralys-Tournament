import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => {
  return <h1>Tournament Manager - Web App</h1>;
};

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
